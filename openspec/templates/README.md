# OpenSpec Templates

Use `change/` when creating a new feature change. These templates standardize
the required document shape; the instantiated change documents are the
authoritative feature specification after acceptance.
